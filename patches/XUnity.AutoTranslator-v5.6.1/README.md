# XUnity.AutoTranslator compatibility patch

Apply `PluginLoader.patch` to the exact upstream revision documented in
`UPSTREAM.md`. The patch is distributed under the same MIT License as
XUnity.AutoTranslator.

The already-tested compatibility binary should not be silently replaced with a
new build unless that new build is tested again in a clean game installation.
